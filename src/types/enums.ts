export enum UserRole {
  Doctor = "Doctor",
  Patient = "Patient",
}

// Enum for gender
export enum Gender {
  Male = "Male",
  Female = "Female",
  Other = "Other",
}

// Enum for consent types
export enum ConsentType {
  Read = "Read",
  Write = "Write",
  Share = "Share",
}
